pipeline {
    agent any
    stages {
              parallel {
		stages{
	             stage("Terraform") {
			 steps {
			       git 'https://github.com/HariDEV-GIT/terra_test.git'	
			 }
		     }
		     stage("Terraform 1") {
			 steps {
			       script {
                        	  def fmtStatus = sh "terraform fmt -list=true -write=false -diff=true -check=true"
                        	  if (fmtStatus == 0) {
	                            echo "Format is Okayt"
                                  } 
                  	          else {
                            	    echo "Code Format Failed"   
                        	  }
                               }	
			 }
		     }
              }
     }
}
